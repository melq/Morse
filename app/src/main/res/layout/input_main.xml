<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/rounded_corner_4"
    android:backgroundTint="@color/white"
    android:layout_margin="6dp"
    android:elevation="6dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageButton
            android:id="@+id/clear_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="@id/et_input_main"
            app:layout_constraintRight_toRightOf="@id/et_input_main"
            android:layout_margin="4dp"
            android:background="@color/white"
            android:src="@drawable/clear"
            android:contentDescription="@string/clear_button" />

        <EditText
            android:id="@+id/et_input_main"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            android:layout_marginTop="8dp"
            android:layout_marginHorizontal="8dp"
            android:autofillHints="@string/input_hint"
            android:hint="@string/input_hint"
            android:inputType="text" />

        <include layout="@layout/switch_input_main"
            app:layout_constraintBottom_toBottomOf="parent"
            android:id="@+id/sw_input_main" />


    </androidx.constraintlayout.widget.ConstraintLayout>


</LinearLayout>